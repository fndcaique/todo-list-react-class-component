import React, { Component } from 'react';

export default class TodoList extends Component {
  render() {
    // const {
    // desestruture o array com todas as tarefas;
    // desestruture a função que resolve a alteração de status das tarefas
    // } = this.props;
    return (
      <ul>
        {
          // liste todas as tarefas
          // passe para cada uma das tarefas a sua descrição
          // passe para cada uma das tarefas o seu status
          // passe para cada uma das tarefas a a função que resolve a alteração de status
        }
      </ul>
    );
  }
}
